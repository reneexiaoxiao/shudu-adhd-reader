# 隐私说明

舒读的排版与语义强调在浏览器本地处理。智能译读是可选功能，只有用户主动开启时才会把当前阅读区的英文段落发送给用户自己配置的智谱 API。

## 数据处理

- 扩展读取目标页面的正文 DOM，用于调整宽度、字体、字号、间距与语义强调。
- 阅读设置保存在浏览器提供的 `chrome.storage.sync` 中；是否跨设备同步由浏览器账户设置决定。
- 智谱 API Key、译读档位、模型代码和译文缓存保存在 `chrome.storage.local` 中，不使用同步存储。已保存的 Key 不在面板中回显，也不会注入网页脚本。
- 开启智能译读后，扩展会向 `https://open.bigmodel.cn/api/paas/v4/chat/completions` 发送当前阅读区的英文段落、页面标题和页面语言；不发送网页 URL、Cookie、浏览历史、表单内容或密码。
- 本地译文缓存只保存正文哈希、译文和学习词，不保存网页 URL；缓存最多保留有限条目并自动淘汰旧记录。
- 扩展不包含自建服务器、第三方分析 SDK、广告、追踪像素或遥测。
- 扩展不读取浏览历史、Cookie、密码、表单输入或下载内容。

## 权限用途

- `storage`：保存扩展开关和阅读参数。
- `activeTab`、`scripting`：仅在用户主动把舒读应用到普通网页时注入本地脚本和样式。
- `mp.weixin.qq.com`、即刻与 X / Twitter 的站点权限：在这些明确支持的站点自动运行。
- `open.bigmodel.cn`：仅用于用户主动发起的连接测试与智能译读请求。

## 删除数据

在扩展面板暂停滚动译读可停止新的 API 请求；「清除译文」会移除当前页面已插入的译文；「删除 Key」会删除本机保存的 API Key。卸载扩展会删除该扩展的本地存储。浏览器账户可能保留同步的排版设置，具体取决于浏览器的同步机制。

如发现隐私或安全问题，请通过 GitHub Issue 提交不含个人信息的复现说明。
