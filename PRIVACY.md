# 隐私说明

## 浏览器本地处理

排版、正文识别和规则语义强调在浏览器内执行。收藏与划线脚本可在 HTTP(S) 页面读取正文和选区；排版仅在支持站点自动启用。阅读设置使用 `chrome.storage.sync`，是否跨设备同步取决于浏览器。划线、批注、API Key 和译文缓存使用 `chrome.storage.local`。

扩展不主动读取浏览历史、Cookie 或密码，不包含遥测、广告或第三方分析 SDK。API Key 存储不等同于加密保险库；获得本机浏览器配置访问权的人可能读取它。

## 外部与本机请求

公开版默认关闭“收藏同步到 Obsidian”。关闭时划线与评论仅保存在扩展本地存储，不调用收藏服务，不提取图片用于收藏，不启动收藏导入任务；开启智谱译读仍会独立请求智谱。关闭开关不会撤回已发出的请求或删除历史数据。

- 主动开启译读或测试连接时，扩展访问智谱开放平台。译读发送当前阅读区的英文段落、页面标题、语言及此前遇到的学习表达；不会发送 Cookie 或 API Key 给文章网站。智谱 API Key 作为认证凭据发送到智谱。
- 收藏、批注同步、库目录查询以及启用的导入流程与 `http://127.0.0.1:8765` 通信。收藏可发送页面 URL、标题、正文、选区、上下文、所选图片及个人备注。
- 为保存原图，扩展可能请求图片源站；视频页面的字幕提取可能请求源站提供的字幕地址。这些站点可看到正常的网络请求信息。
- 启用个性化分析或辅助消化后，本机服务是否调用额外模型、保存到哪里，由服务实现和用户配置决定。本仓库不附带作者的服务、画像或知识库。

## 权限

- `storage`：设置、API 配置、译文缓存和本地批注。
- `activeTab`、`scripting`：按需向当前网页注入扩展代码。
- HTTP(S) 主机权限：跨站正文、图片提取、划线和收藏；不表示所有网站都会被重排。
- `contextMenus`、`notifications`：右键收藏和操作结果。
- `downloads`：可选视频导入模块检查下载记录。公开版不配置作者私人视频助手的扩展 ID，不自动接管任意下载。
- `alarms`：可选集成任务调度；公开版不默认启动作者私人微信读书同步任务。

## 数据删除与备份

“清除译文”只移除当前网页的译文，不等于清空译文缓存。“删除 Key”删除本机保存的 Key。卸载扩展会移除其本地存储，可能丢失划线与批注；不会删除已写入 Obsidian 的文件。浏览器同步的设置按浏览器账户规则处理。本机服务中的数据需到其实际保存位置单独管理。
