# 安全说明

- 不在 Issue、日志、示例或 Git 提交里粘贴密钥。若密钥曾在不可信位置暴露，应在提供商后台撤销并重新生成。
- 本机收藏服务只能绑定回环地址。服务端应验证 Origin、请求类型、体积、路径、附件 MIME 和访问权限；不要因为来自 localhost 就信任请求。
- 不要让服务端执行网页给出的命令。网页、文件名、字幕和模型输出均为不可信内容。
- 本仓库不包含生产可用的收藏服务；`docs/companion-api.md` 是客户端契约，不是服务端安全实现。
- 请通过仓库 Security 页中可用的私密报告渠道报告漏洞；如果未启用私密报告，可先提交不含利用细节和个人数据的 Issue 联系维护者。
