# 参与舒读开发

舒读的公共代码在本仓库维护。通用阅读、译读和批注功能应能在没有作者私人服务或配置的环境里使用。

## 分支与提交

- `main`：稳定代码，正式 Release 从验证后的提交生成。
- `opensource`：开源维护与集成分支。
- 功能和修复从最新 `opensource` 建立短期分支，通过 PR 合入；每个 PR 尽量只解决一个问题，附复现步骤、修改范围和验证结果。
- 发布时由维护者将经过验证的 `opensource` 通过 PR 合入 `main`。不要直接用旧快照覆盖分支，也不要强推重写公共历史。

如果公共仓库收到新提交，先合并最新变更并解决冲突，再继续开发。个人定制应放在自己的私有配置或适配层；通用修复先贡献到公共代码，再同步回个人版本，不把私人仓库历史或完整知识库反向合入这里。

## 本地验证

扩展无需构建。测试使用 Node.js 22 或更高版本：

```sh
node --test tests/*.test.mjs collector/*.test.mjs
```

CI 对 push 和 PR 运行同一套测试。测试通过不代表真实站点和本机服务已经端到端验证；请在 PR 中分别写清已验证和未验证的部分。

按改动范围至少检查：

- **无服务模式**：不配置 Obsidian、不启动收藏服务，仍能创建、编辑和恢复本机划线与评论；关闭收藏同步时不调用收藏接口，已有数据不被删除。
- **选择菜单**：默认是选区旁的小球，点击才展开；不阻挡原生选择菜单，不跳到屏幕边缘。
- **译读与排版分离**：普通英文网页只开启译读时不改变原有排版；不擅自恢复 X / Twitter 排版注入。
- **可选同步**：服务断开、超时或仅部分成功时准确反馈，不把本地保存当成 Obsidian 同步成功；不要覆盖用户手写笔记。
- **界面与回归**：检查相关正常、错误、长文本、窄屏和深色状态；为修复新增针对性测试。

仅改文档时，核对链接和能力描述即可，不需要伪造浏览器或同步测试结果。

## 隐私与配置边界

所有分支、提交、PR、Issue、日志附件和 Release 包都属于公开内容。提交前检查 diff 与附件，不要包含：

- API Key、令牌、私钥、Cookie、浏览器配置或真实存储导出；
- 个人画像正文、知识库原文、用户笔记、私人文件路径或内部链接；
- 未获再分发授权的字体、图片或其他资产。

即使另有私有仓库，凭据也应由本机配置读取，不提交到 Git。示例使用虚构数据和占位符；不要通过真实笔记复现问题。安全问题的报告要求见 [SECURITY.md](SECURITY.md)。

## 可选服务能力

当前公开版发布的是扩展客户端，尚未提供通用 Obsidian 服务安装包、Vault 选择器或个人画像编辑界面。接口约定见 [本机服务接入](docs/companion-api.md)。

新增这些能力时应让用户显式配置自己的目录，画像默认留空且可选，不依赖作者的分类或私人服务。不支持的能力要准确提示或隐藏，不能仅增加按钮就标为可用。涉及文件写入时需限制目标范围、处理错误，并验证既有笔记不会被覆盖。
